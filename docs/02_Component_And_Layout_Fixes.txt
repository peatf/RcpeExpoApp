# Prompt: Fix Component and Layout Issues Across All Screens

As an AI developer, your task is to correct specific component and layout issues on various screens. These fixes will ensure that the UI is correctly aligned, properly spaced, and visually consistent with the app's design system.

---

### **Home / Dashboard**

- **Issue 4: Central Circular Button Too Small / High**
  - **File / Location:** `src/screens/Main/DashboardScreen.tsx` (style: `startButton`)
  - **Suggestion:** Increase the `width` and `height` of the button and lower it with `marginTop` or by using Flexbox's `justifyContent: "center"`.
- **Issue 5: Remove Tagline Text**
  - **File / Location:** `src/screens/Main/WelcomeScreen.tsx`
  - **Suggestion:** Delete the `<Text>` nodes containing "Reality / Creation / Perception Engine".

---

### **Frequency Mapper**

- **Issue 6: Step-Bar Has Unwanted Background**
  - **File / Location:** `src/components/StepTracker.tsx` (`styles.container.backgroundColor`)
  - **Suggestion:** Set the background color to `"transparent"`.
- **Issue 11: Page Content Sits Too High**
  - **File / Location:** `src/screens/Main/FrequencyMapperScreen/index.tsx` (root `styles.container`)
  - **Suggestion:** Add `paddingTop` to the container. The value should be calculated based on the header height plus standard spacing (e.g., `headerHeight + spacing.md`).
- **Issue 13: Result Card Styling Off**
  - **File / Location:** `src/screens/Main/FrequencyMapperScreen/index.tsx` (in the result render function)
  - **Suggestion:** Apply the standard neumorphic shadow and border-radius from the theme (e.g., `theme.shadows.neumorph`).

---

### **Oracle**

- **Issue 19: Square Button Without Label**
  - **File / Location:** `src/screens/Main/OracleScreen.tsx` (`styles.sendButton`)
  - **Suggestion:** Change the button's `shape` prop to `"rectangle"` and add the label "Ask".
- **Issue 20: Layout Too High**
  - **File / Location:** `src/screens/Main/OracleScreen.tsx` (root `View` style)
  - **Suggestion:** Wrap the content in a `SafeAreaView` or add `paddingTop` to prevent it from sitting under the device's notch.

---

### **Base Chart**

- **Issue 22: Remove "Centers / Active Gates" Cards**
  - **File / Location:** `src/screens/Main/UserBaseChartScreen.tsx`
  - **Suggestion:** Delete the JSX blocks that render these superfluous info cards.
- **Issue 23: Basic Info Card Lacks Astro Trio**
  - **File / Location:** `src/screens/Main/UserBaseChartScreen.tsx` (`renderBasicInfo()` function)
  - **Suggestion:** Extend the data fetch and display logic to include the user's Sun, Moon, Rising signs, and Chart Ruler.
- **Issue 24: "Refresh Data" Button Is Pointless**
  - **File / Location:** `src/screens/Main/UserBaseChartScreen.tsx` (at the bottom of the JSX)
  - **Suggestion:** Remove this button until the backend functionality is ready.
- **Issue 26: Beige Background on Chart**
  - **File / Location:** `src/components/BaseChart/ChartCanvas.tsx` or `src/components/EnergeticBlueprint/BlueprintCanvas.tsx`
  - **Suggestion:** Set the component's `backgroundColor` to `"transparent"`.

---

### **Living Log & Decision Maker**

- **Issue 31 & 34: Beige Background**
  - **File / Location:** `src/screens/Main/HumanDesignTools/LivingLogScreen.tsx`, `src/screens/Main/DecisionMakerScreen.tsx`
  - **Suggestion:** Remove the `backgroundColor` from the screen's root style.
- **Issue 32: Square Unlabeled Button**
  - **File / Location:** `src/screens/Main/HumanDesignTools/LivingLogScreen.tsx` (`styles.createButton`)
  - **Suggestion:** Replace the current button with a rectangle-shaped `StackedButton` and give it the text "Log Entry".