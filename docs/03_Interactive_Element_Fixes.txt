# Prompt: Repair Broken and Inconsistent Interactive Elements

As an AI developer, your task is to fix broken interactive elements across the application. This includes text inputs, button behaviors, and navigation flows. Your goal is to make all user interactions reliable and consistent.

---

### **Frequency Mapper**

- **Issue 7: Input Stops After One Character**
  - **File / Location:** `src/screens/Main/FrequencyMapperScreen/index.tsx` (lines 49-55)
  - **Suggestion:** Debug the state update logic for the `TextInput`. Ensure the state is not being reset on every render and that there is no incorrect logic like `.substring(0,1)`.
- **Issue 8: White Rectangle Behind Input Label**
  - **File / Location:** `src/screens/Main/FrequencyMapperScreen/index.tsx` (`styles.prompt` / `styles.input`)
  - **Suggestion:** Remove the background color from the label's style or use `position: "absolute"` to create a proper floating label effect.
- **Issue 9: Border Too Light on TextInput**
  - **File / Location:** `src/screens/Main/FrequencyMapperScreen/index.tsx` (`styles.input.borderColor`)
  - **Suggestion:** Switch the border color from `theme.colors.base1` to a darker, more visible color like `theme.colors.base3`.
- **Issue 10: Wrong Button Component ("Process & Map")**
  - **File / Location:** `src/screens/Main/FrequencyMapperScreen/index.tsx` (inside `buttonContainer`)
  - **Suggestion:** Replace the plain `TouchableOpacity` with the app's standard `<StackedButton shape="rectangle">`.
- **Issue 15: "Start over" Label & Flow Wrong**
  - **File / Location:** `src/screens/Main/FrequencyMapperScreen/index.tsx` (navigation handler)
  - **Suggestion:** Change the button's label from "Start Over" to "Start Calibration" and update the `onPress` handler to navigate to the `CalibrationToolScreen` (e.g., `navigation.navigate('CalibrationTool')`).

---

### **Oracle**

- **Issue 18: Input Limited to One Character**
  - **File / Location:** `src/screens/Main/OracleScreen.tsx`
  - **Suggestion:** This is likely the same root cause as Issue 7. Investigate the component for an incorrect `maxLength={1}` prop or a faulty `value` binding in the `TextInput`.

---

### **Living Log**

- **Issue 30: TextInput Single-Character Bug & Label Overlay**
  - **File / Location:** `src/screens/Main/HumanDesignTools/LivingLogScreen.tsx` (in the header form)
  - **Suggestion:** Fix the `TextInput` state logic to allow for multi-character input. For the label, adjust its styling so it does not draw over the input's border, adopting the floating label pattern used in other tools.

---

### **Decision Maker Tools**

- **Issue 35: Tool Tabs Not Clickable**
  - **File / Location:** `src/components/DecisionMakerTab.tsx` and its parent `src/screens/Main/DecisionMakerScreen.tsx`
  - **Suggestion:** Ensure the `DecisionMakerTab` component receives and properly handles an `onPress` or `onSelect` callback prop. Verify that the parent screen correctly maps this callback to all rendered tabs.